name: code analysis

on:
  workflow_call:
    secrets:
      REPO_ACCOUNT:
        required: true
      REPO_ACCESS_TOKEN:
        required: true
    outputs:
      comment:
        value: ${{ jobs.code_analysis.outputs.comment }}
env:
  GITHUB_ACCOUNT: ${{ secrets.REPO_ACCOUNT }}
  GITHUB_PKG_NFW_AUTH_TOKEN: ${{ secrets.REPO_ACCESS_TOKEN }}

jobs:
  code_analysis:
    runs-on: ubuntu-latest
    outputs:
      comment: ${{ steps.comment.outputs.result }}

    steps:
      - name: 💼 Checkout
        uses: actions/checkout@v4

      - name: ⚡ Setup Nuget Cache
        uses: actions/cache@v3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: 🖥️ Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.x

      - name: 🕵️ Code Analysis
        id: code_analysis
        run: |
          cd ./backend/NeuroFramework.Common/
          dotnet sln Common.sln remove tests/**/*.csproj
          dotnet restore --configfile "github-actions-nuget.config"
          dotnet build -m --no-restore -p:Configuration=Release -p:EnforceCodeStyleInBuild=true -p:TreatWarningsAsErrors=true -p:IsGithubActions=true > CodeAnalysis.log

      - name: 🖊️ Create Comment
        id: comment
        if: always()
        uses: actions/github-script@v7
        with:
          result-encoding: string
          script: |
            const { readFileSync } = require('fs');
            let contents = readFileSync('./backend/NeuroFramework.Common/CodeAnalysis.log').toString();
            console.log(contents);
            contents = contents.replace("/home/runner/work/neuro-framework-common/neuro-framework-common/backend/NeuroFramework.Common/", "");
            contents = contents.replace("src/", "");
            contents = contents.match(/FAILED[\s\S]*Error/g);
            if (contents) {
              contents = contents.replace(/\n/g, '\\n');
            }else {
              contents = "- :ok_woman: Code Analysis passed";
            }
            return "### :information_desk_person: Code Analysis\\n\\n" + contents + "\\n\\n";
