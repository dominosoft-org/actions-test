name: run on changing pnpm-lock.yaml

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      license-check:
        value: ${{ jobs.frontend-license-check.outputs.result }}

jobs:
  frontend-license-check:
    name: Frontend License Check✅
    uses: ./.github/workflows/frontend-license-check.yaml
